rootProject.name='conductor'

include 'client','common','contribs','core', 'es5-persistence','jersey', 'kafka-persistence', 'redis-lock'
include 'redis-persistence','server','ui'
include 'grpc', 'grpc-server', 'grpc-client'

rootProject.children.each {it.name="conductor-${it.name}"}
